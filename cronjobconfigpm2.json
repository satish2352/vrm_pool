module.exports = {
    apps: [
      {
        name: 'scheduler cronjob nodejs', // Name of the application
        script: 'node scheduler.js', // Path to your Node.js script
        autorestart: true, // Set to true to automatically restart the script if it crashes
        max_restarts: 10, // Maximum number of restarts within 60 seconds (to prevent constant restarts if the script keeps crashing)
        error_file: 'logs/error.log', // Path to error log file
        out_file: 'logs/output.log', // Path to output log file
       
      }
    ]
  };
  